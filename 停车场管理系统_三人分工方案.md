# 立体多层停车场管理系统 — 三人分工方案

> 课程：数据结构实践  
> 语言：C 语言  
> 核心要求：车位分配/释放 O(1)、排队轮候、实时查找、费用计算、收入统计

---

## 一、系统架构总览

```
┌──────────────────────────────────────────────────────────────┐
│                       main.c (程序入口)                       │
│               菜单循环 + 调度各模块                             │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐     │
│  │     pzx      │  │  yjk（组长）  │  │     zxc      │     │
│  │   数据结构层    │  │   业务逻辑层   │  │   统计与UI层   │     │
│  │               │  │               │  │               │     │
│  │   ds.h/ds.c   │  │  parking.h/c  │  │  stats.h/c    │     │
│  │   (结构体定义)  │  │  fee.h/c      │  │  ui.h/c       │     │
│  │   栈 / 队列    │  │  (入场/出场    │  │  file_io.h/c  │     │
│  │   哈希表       │  │   计费/查找)   │  │  (菜单/报表    │     │
│  │               │  │               │  │   持久化)      │     │
│  └───────────────┘  └───────────────┘  └───────────────┘     │
│                                                              │
│                  共享头文件: ds.h (pzx 编写)                  │
├──────────────────────────────────────────────────────────────┤
│                       主控流程                                │
│  入口登记 → 分配车位 / 排队 → 出口结算 → 释放车位 → 叫号      │
└──────────────────────────────────────────────────────────────┘
```

---

## 二、核心数据结构设计

### 2.1 停车场三层模型

```
3F  [●][●][●][○][○]     ● 已占用
    [○][○][●][○][○]     ○ 空闲
    [○][○][○][○][○]
    [○][○][○][○][○]

2F  [●][●][●][●][○]
    [○][○][●][○][○]
    [○][○][○][○][○]
    [○][○][○][○][○]

1F  [●][●][●][●][●]  ← 已满
    [●][○][○][○][○]
    [○][○][○][○][○]
    [○][○][○][○][○]
```

- 每层 4 排 × 5 列 = 20 个车位
- 共 3 层 × 20 = **60 个车位**
- 每个车位用三维坐标 `(floor, row, col)` 唯一标识

### 2.2 核心结构体定义 (均在 ds.h 中)

```c
// ========== 车位状态 ==========
typedef enum { SPOT_FREE, SPOT_OCCUPIED } SpotStatus;
typedef enum { ENTRY_SUCCESS, PARKING_FULL, PLATE_EXISTS } EntryResult;
typedef enum { EXIT_SUCCESS, NOT_FOUND, NOT_PARKED } ExitResult;

// ========== 3D 坐标 ==========
typedef struct { int floor, row, col; } SpotLocation;

// ========== 车位 ==========
typedef struct {
    SpotLocation loc;        // 位置坐标
    SpotStatus status;       // 空闲/占用
    char plate[16];          // 车牌号，空闲时为 ""
} ParkingSpot;

// ========== 车辆在场记录（存哈希表中） ==========
typedef struct {
    char plate[16];          // 车牌号
    SpotLocation loc;        // 停放位置
    time_t entry_time;       // 入场时间戳
} VehicleRecord;

// ========== 空闲车位栈（O(1)分配核心） ==========
typedef struct {
    SpotLocation *spots;     // 动态数组
    int top;                 // 栈顶指针
    int capacity;            // 容量
} FreeSpotStack;

// ========== 等待队列（FIFO） ==========
typedef struct QueueNode {
    char plate[16];
    time_t arrival_time;
    struct QueueNode *next;
} QueueNode;

typedef struct {
    QueueNode *front, *rear;
    int count;
} WaitingQueue;

// ========== 哈希表（O(1)车牌查找） ==========
#define HT_SIZE 10007        // 质数，减少哈希冲突

typedef struct HTNode {
    char plate[16];
    VehicleRecord *record;
    struct HTNode *next;
} HTNode;

typedef struct {
    HTNode *buckets[HT_SIZE];
    int count;
} HashMap;

// ========== 计费规则 ==========
typedef struct {
    double base_fee;         // 起步价（元）
    int free_minutes;         // 免费分钟数
    double rate_per_hour;    // 超出后每小时收费（元）
    double max_daily;        // 每日封顶（元）
} FeeRule;

// ========== 历史交易 ==========
typedef struct {
    char plate[16];
    SpotLocation loc;
    time_t entry_time;
    time_t exit_time;
    int duration_minutes;
    double fee;
} TransactionRecord;

// ========== 全局系统状态（一个结构体包含所有） ==========
typedef struct {
    ParkingSpot spots[3][4][5];    // 3层×4排×5列
    HashMap plate_index;            // 车牌 → VehicleRecord*
    FreeSpotStack free_spots;       // 空闲车位栈
    WaitingQueue wait_queue;        // 等待队列
    TransactionRecord *history;     // 历史交易（动态数组）
    int history_count;              // 当前历史记录数
    int history_capacity;           // 动态数组容量
    FeeRule fee_rule;               // 计费规则
    double total_revenue;           // 总收入
} ParkingSystem;
```

---

## 三、关键算法：O(1) 车位分配/释放

### 核心思想：空闲车位栈

```
初始化时：将所有车位坐标依次入栈
          ┌──────┬──────┬──────┬──────┬──────┐
  栈顶 →  │0,0,0 │0,0,1 │0,0,2 │ ...  │2,3,4 │  ← 栈底
          └──────┴──────┴──────┴──────┴──────┘

车辆入场：pop() → 取出一个空闲坐标 → O(1)
车辆出场：push(该车位坐标) → 放回栈顶 → O(1)

等待队列（链式 FIFO）：
  front → [京A123] → [京B456] → [京C789] → rear
           (先到)                          (后到)

出场释放车位后 → 队列非空 → 出队 → 立即分配该车位 → O(1)
```

**为什么这是 O(1)**：
- 栈的 pop/push 操作只有指针/下标移动，不涉及遍历
- 哈希表查找也是 O(1) 平均（拉链法，质数桶大小避免聚集）
- 完全满足题目"分配与释放时间复杂度尽量低"的要求

---

## 四、文件结构

```
综合性实验/
├── ds.h              # pzx: 数据结构定义 + 函数声明
├── ds.c              # pzx: 栈/队列/哈希表实现
├── parking.h         # yjk: 停车场业务函数声明
├── parking.c         # yjk: 入场/出场/查找/排队
├── fee.h             # yjk: 计费模块声明
├── fee.c             # yjk: 计费逻辑
├── stats.h           # zxc: 统计模块声明
├── stats.c           # zxc: 统计实现
├── ui.h              # zxc: 菜单界面声明
├── ui.c              # zxc: 菜单 + 交互实现
├── file_io.h         # zxc: 文件读写声明
├── file_io.c         # zxc: 数据持久化
├── main.c            # zxc: 程序入口
└── Makefile          # (可选) 编译脚本
```

---

## 五、pzx — 数据结构层（底层基础设施）

### 5.1 负责文件
**ds.h** 和 **ds.c**

### 5.2 需要实现的全部函数

```c
// ---- 初始化与销毁 ----
void ds_init(ParkingSystem *ps);
    /* 功能：初始化停车场系统
     * 1. 所有车位状态设为 FREE，plate 置空
     * 2. 初始化空闲栈，将所有车位坐标依次入栈
     * 3. 初始化哈希表（所有桶置 NULL）
     * 4. 初始化等待队列（front=rear=NULL）
     * 5. 初始化历史记录动态数组（初始容量 100）
     * 6. 设置默认计费规则：
     *      base_fee=5.0, free_minutes=15,
     *      rate_per_hour=4.0, max_daily=40.0
     * 7. total_revenue = 0.0
     */

void ds_destroy(ParkingSystem *ps);
    /* 功能：释放所有动态内存
     * 1. free(空闲栈.spots)
     * 2. 遍历哈希表每个桶，释放所有节点
     * 3. 释放等待队列所有节点
     * 4. free(history)
     */

// ---- 空闲车位栈 ----
void stack_init(FreeSpotStack *s, int capacity);
    /* 功能：初始化栈，分配 capacity 个 SpotLocation 空间
     * 实现：s->spots = malloc(...), s->top = -1
     */

void stack_push(FreeSpotStack *s, SpotLocation loc);
    /* 功能：将车位坐标入栈
     * 实现：s->spots[++s->top] = loc
     * 注意：如果 top+1 >= capacity，realloc 扩容为 2 倍
     */

SpotLocation stack_pop(FreeSpotStack *s);
    /* 功能：弹出栈顶车位坐标
     * 实现：return s->spots[s->top--]
     * 前置条件：!stack_is_empty(s)，否则 abort
     */

int stack_is_empty(FreeSpotStack *s);
    /* 功能：检查栈是否为空
     * 返回：s->top == -1
     */

int stack_size(FreeSpotStack *s);
    /* 功能：返回当前空闲车位数
     * 返回：s->top + 1
     */

// ---- 等待队列 ----
void queue_init(WaitingQueue *q);
    /* 功能：初始化空队列
     * 实现：q->front = q->rear = NULL; q->count = 0;
     */

void queue_enqueue(WaitingQueue *q, const char *plate, time_t t);
    /* 功能：将车辆加入队尾
     * 实现：创建 QueueNode 节点，strcpy 车牌，记录时间
     *       如果队列空 → front = rear = newNode
     *       否则 → rear->next = newNode; rear = newNode
     *       count++
     */

int queue_dequeue(WaitingQueue *q, char *out_plate, time_t *out_t);
    /* 功能：从队首取出一辆车
     * 返回：1=成功取出，0=队列空
     * 实现：保存 front 节点，strcpy 车牌和时间
     *       front = front->next，free 旧节点
     *       如果队列变空 → rear = NULL
     *       count--
     */

int queue_is_empty(WaitingQueue *q);
    /* 功能：判断队列是否为空
     * 返回：q->front == NULL 或 q->count == 0
     */

int queue_count(WaitingQueue *q);
    /* 功能：返回排队车辆数
     * 返回：q->count
     */

// ---- 哈希表 ----
unsigned int hash_str(const char *str);
    /* 功能：计算字符串哈希值
     * 算法：unsigned long h = 0;
     *       while (*str) h = h * 31 + (unsigned char)(*str++);
     *       return h % HT_SIZE;
     * 原理：31 是常用质数乘子，分布均匀
     */

void ht_init(HashMap *ht);
    /* 功能：初始化哈希表
     * 实现：for i=0..HT_SIZE-1: ht->buckets[i] = NULL
     *       ht->count = 0
     */

void ht_insert(HashMap *ht, VehicleRecord *rec);
    /* 功能：插入车辆记录
     * 实现：计算 hash = hash_str(rec->plate)
     *       创建 HTNode，strcpy 车牌，指向 record
     *       头插法插入 buckets[hash]
     *       ht->count++
     * 注意：不需要检查重复（调用方保证不重复）
     */

VehicleRecord *ht_find(HashMap *ht, const char *plate);
    /* 功能：通过车牌查找车辆在场记录
     * 实现：hash = hash_str(plate)
     *       遍历 buckets[hash] 链表，strcmp 匹配
     *       找到 → 返回 HTNode->record
     *       未找到 → return NULL
     */

void ht_remove(HashMap *ht, const char *plate);
    /* 功能：删除车辆记录（出场时调用）
     * 实现：hash = hash_str(plate)
     *       遍历链表，找到匹配节点
     *       修改前驱节点的 next 跳过该节点
     *       free HTNode（注意：只释放 HTNode，不释放 VehicleRecord）
     *       VehicleRecord 由调用方负责释放
     *       ht->count--
     * 注意：未找到时什么都不做
     */

void ht_clear(HashMap *ht);
    /* 功能：清空哈希表（销毁时调用）
     * 实现：遍历每个桶，free 所有 HTNode
     */

// ---- 辅助函数 ----
SpotLocation make_loc(int f, int r, int c);
    /* 功能：创建一个 SpotLocation 结构体 */

int loc_equal(SpotLocation a, SpotLocation b);
    /* 功能：判断两个坐标是否相等 */
```

### 5.3 具体实现要点

#### 空闲栈初始化（ds_init 中关键部分）

```c
// 初始化空闲车位栈：将所有车位坐标入栈
stack_init(&ps->free_spots, TOTAL_SPOTS);
for (int f = 0; f < FLOORS; f++) {
    for (int r = 0; r < ROWS; r++) {
        for (int c = 0; c < COLS; c++) {
            stack_push(&ps->free_spots, make_loc(f, r, c));
            ps->spots[f][r][c].loc = make_loc(f, r, c);
            ps->spots[f][r][c].status = SPOT_FREE;
            ps->spots[f][r][c].plate[0] = '\0';
        }
    }
}
```

#### 哈希函数（保证均匀分布）

```c
unsigned int hash_str(const char *str) {
    unsigned long hash = 0;
    while (*str) {
        hash = hash * 31 + (unsigned char)(*str++);
    }
    return hash % HT_SIZE;
}
```

选择 31 的原因：乘法可被编译器优化为 `(hash << 5) - hash`，效率高。

---

## 六、yjk（组长） — 业务逻辑层（核心流程）

### 6.1 负责文件
**parking.h / parking.c** 和 **fee.h / fee.c**

### 6.2 入场逻辑

```c
// parking.c

EntryResult vehicle_entry(ParkingSystem *ps, const char *plate) {
    // 1. 校验车牌号是否合法（非空、长度）
    if (plate == NULL || strlen(plate) == 0) return ...;

    // 2. 查哈希表：该车牌是否已在停车场内
    if (ht_find(&ps->plate_index, plate) != NULL) {
        printf("该车辆已在停车场内！\n");
        return PLATE_EXISTS;
    }

    // 3. 检查空闲车位栈
    if (!stack_is_empty(&ps->free_spots)) {
        // 3a. 有空位 — 分配车位
        SpotLocation loc = stack_pop(&ps->free_spots);

        // 标记车位占用
        ps->spots[loc.floor][loc.row][loc.col].status = SPOT_OCCUPIED;
        strcpy(ps->spots[loc.floor][loc.row][loc.col].plate, plate);

        // 创建车辆记录
        VehicleRecord *rec = (VehicleRecord *)malloc(sizeof(VehicleRecord));
        strcpy(rec->plate, plate);
        rec->loc = loc;
        rec->entry_time = time(NULL);

        // 插入哈希表
        ht_insert(&ps->plate_index, rec);

        printf("车辆 %s 入场，车位：%dF-%d排-%d列\n",
               plate, loc.floor+1, loc.row+1, loc.col+1);
        return ENTRY_SUCCESS;
    } else {
        // 3b. 无空位 — 排队
        queue_enqueue(&ps->wait_queue, plate, time(NULL));
        printf("停车场已满，车辆 %s 已加入等待队列（第 %d 位）\n",
               plate, ps->wait_queue.count);
        return PARKING_FULL;
    }
}
```

### 6.3 出场逻辑

```c
// parking.c

ExitResult vehicle_exit(ParkingSystem *ps, const char *plate, double *out_fee) {
    // 1. 查找车辆
    VehicleRecord *rec = ht_find(&ps->plate_index, plate);
    if (rec == NULL) {
        printf("未找到该车辆！\n");
        return NOT_FOUND;
    }

    // 2. 记录位置
    SpotLocation loc = rec->loc;

    // 3. 计算停车时长和费用
    time_t now = time(NULL);
    int duration = (int)difftime(now, rec->entry_time) / 60; // 分钟
    double fee = calculate_fee(&ps->fee_rule, duration);

    // 4. 释放车位
    ps->spots[loc.floor][loc.row][loc.col].status = SPOT_FREE;
    ps->spots[loc.floor][loc.row][loc.col].plate[0] = '\0';

    // 5. 将车位放回空闲栈
    stack_push(&ps->free_spots, loc);

    // 6. 从哈希表删除记录
    ht_remove(&ps->plate_index, plate);

    // 7. 记录历史交易
    // 扩容判断 (history_count == history_capacity 时 realloc)
    TransactionRecord *tr = &ps->history[ps->history_count++];
    strcpy(tr->plate, plate);
    tr->loc = loc;
    tr->entry_time = rec->entry_time;
    tr->exit_time = now;
    tr->duration_minutes = duration;
    tr->fee = fee;

    // 8. 累计收入
    ps->total_revenue += fee;

    // 9. ★★★ 释放 VehicleRecord 的内存 ★★★
    free(rec);

    // 10. ★★★ 关键：尝试从队列调度下一辆车 ★★★
    try_serve_queue(ps, loc);   // 把刚释放的车位给排队车辆

    // 11. 打印出场信息
    printf("车辆 %s 出场\n", plate);
    printf("入场时间：%s", ctime(&tr->entry_time));
    printf("出场时间：%s", ctime(&tr->exit_time));
    printf("停车时长：%d 分钟\n", duration);
    printf("应收费用：%.2f 元\n", fee);

    *out_fee = fee;
    return EXIT_SUCCESS;
}
```

### 6.4 "出一个进一个" — 排队调度

```c
// parking.c — 静态函数，不暴露到头文件

static void try_serve_queue(ParkingSystem *ps, SpotLocation freed_loc) {
    // 队列空 → 直接返回
    if (queue_is_empty(&ps->wait_queue)) return;

    // 出队一辆车
    char plate[16];
    time_t arrival_time;
    queue_dequeue(&ps->wait_queue, plate, &arrival_time);

    // 分配刚释放的车位
    ps->spots[freed_loc.floor][freed_loc.row][freed_loc.col].status = SPOT_OCCUPIED;
    strcpy(ps->spots[freed_loc.floor][freed_loc.row][freed_loc.col].plate, plate);

    VehicleRecord *rec = (VehicleRecord *)malloc(sizeof(VehicleRecord));
    strcpy(rec->plate, plate);
    rec->loc = freed_loc;
    rec->entry_time = time(NULL);  // 入场时间 = 当前时间（不是排队时间）

    ht_insert(&ps->plate_index, rec);

    printf("═══════════════════════════════════════\n");
    printf("  排队车辆 %s 已进入停车场！\n", plate);
    printf("  车位：%dF-%d排-%d列\n",
           freed_loc.floor+1, freed_loc.row+1, freed_loc.col+1);
    printf("  预计排队时长：%ld 分钟\n",
           (long)(difftime(time(NULL), arrival_time) / 60));
    printf("═══════════════════════════════════════\n");
}
```

### 6.5 实时查找

```c
// parking.c

VehicleRecord *find_vehicle(ParkingSystem *ps, const char *plate) {
    return ht_find(&ps->plate_index, plate);
}

int find_in_queue(ParkingSystem *ps, const char *plate) {
    int pos = 1;
    QueueNode *p = ps->wait_queue.front;
    while (p != NULL) {
        if (strcmp(p->plate, plate) == 0) return pos;
        p = p->next;
        pos++;
    }
    return -1;  // 未找到
}
```

### 6.6 计费逻辑

```c
// fee.c

double calculate_fee(const FeeRule *rule, int duration_minutes) {
    // 在免费时段内 → 免费
    if (duration_minutes <= rule->free_minutes) {
        return 0.0;
    }

    // 超出部分按小时计费，不足一小时按一小时算（ceil）
    int extra_minutes = duration_minutes - rule->free_minutes;
    int hours = (int)ceil(extra_minutes / 60.0);
    double fee = rule->base_fee + hours * rule->rate_per_hour;

    // 不超过每日封顶
    if (fee > rule->max_daily) {
        fee = rule->max_daily;
    }

    return fee;
}
```

**计费示例**：
| 停车时长 | 免费时长 | base_fee | 费率 | 计算过程 | 费用 |
|---------|---------|---------|------|---------|------|
| 10 分钟 | 15 | 5 | 4/小时 | 在免费时段内 | **0 元** |
| 30 分钟 | 15 | 5 | 4/小时 | 5 + ceil(15/60)×4 = 5+1×4 | **9 元** |
| 2 小时 | 15 | 5 | 4/小时 | 5 + ceil(105/60)×4 = 5+2×4 | **13 元** |

---

## 七、zxc — 统计与交互层

### 7.1 负责文件
**stats.h / stats.c**, **ui.h / ui.c**, **file_io.h / file_io.c**, **main.c**

### 7.2 统计模块 (stats.c)

```c
// ---- 1. 车位占用概览 ----
void stats_occupancy(ParkingSystem *ps) {
    // 遍历 spots[FLOORS][ROWS][COLS]
    // 统计每层：占用数 / 总数
    // 输出格式示例：
    //   楼层 | 占用 | 空闲 | 使用率
    //   ─────┼──────┼──────┼──────
    //   1F   | 15   | 5    | 75.0%
    //   2F   | 10   | 10   | 50.0%
    //   3F   | 8    | 12   | 40.0%
    //   ─────┼──────┼──────┼──────
    //   总计 | 33   | 27   | 55.0%
}

// ---- 2. 收入统计 ----
void stats_revenue(ParkingSystem *ps, int mode) {
    // mode 0: 总收入 — 直接打印 ps->total_revenue
    // mode 1: 今日收入 — 遍历 history，比较 entry_time 的日期
    // mode 2: 本月收入 — 遍历 history，比较年+月
    // mode 3: 上周收入 — 遍历 history，判断是否在上一周范围内
    //
    // 时间判断使用 localtime(&tr->entry_time) → struct tm
    // 比较 tm_year, tm_mon, tm_mday
}

// ---- 3. 历史交易明细 ----
void stats_transactions(ParkingSystem *ps) {
    // 遍历 ps->history[0..history_count-1]
    // 输出每笔：车牌、位置、入场时间、出场时间、时长、费用
    // 分页显示（每页20条），按 q 退出
}

// ---- 4. 指定车牌历史 ----
void stats_plate_history(ParkingSystem *ps, const char *plate) {
    // 遍历 history，strcmp 匹配车牌
    // 输出该车的所有历史记录
    // 如果没找到 → 提示"无记录"
}

// ---- 5. 楼层热度分析 ----
void stats_floor_usage(ParkingSystem *ps) {
    // 遍历 history 数组，统计每层出现次数
    // 计算每层周转率：交易次数 / 该层车位数
    //
    // 输出示例：
    //   楼层热度排名（按周转率）：
    //   1. 1F — 120次 — 周转率 6.0
    //   2. 2F — 85次  — 周转率 4.25
    //   3. 3F — 42次  — 周转率 2.1
}

// ---- 6. 峰值时段分析 ----
void stats_peak_hours(ParkingSystem *ps) {
    // int hour_count[24] = {0};  // 每个时段的入场次数
    // 遍历 history，对每个 entry_time 提取 tm->tm_hour
    // hour_count[tm->tm_hour]++
    //
    // 输出示例：
    //   峰值时段 Top 3：
    //   1. 10:00-11:00 — 32 辆车入场
    //   2. 14:00-15:00 — 28 辆车入场
    //   3. 18:00-19:00 — 25 辆车入场
}
```

### 7.3 菜单界面 (ui.c)

```c
void ui_main_menu(ParkingSystem *ps) {
    int choice;
    do {
        printf("\n");
        printf("╔══════════════════════════════════════╗\n");
        printf("║       立体多层停车场管理系统          ║\n");
        printf("╠══════════════════════════════════════╣\n");
        printf("║  1. 车辆入场登记                     ║\n");
        printf("║  2. 车辆出场结算                     ║\n");
        printf("║  3. 查找车辆                         ║\n");
        printf("║  4. 空闲车位查看                     ║\n");
        printf("║  5. 等待队列查看                     ║\n");
        printf("║  6. 车位状态总览 (ASCII图)            ║\n");
        printf("║  7. 收入统计                         ║\n");
        printf("║  8. 历史交易明细                     ║\n");
        printf("║  9. 楼层热度分析                     ║\n");
        printf("║ 10. 峰值时段分析                     ║\n");
        printf("║ 11. 保存数据                         ║\n");
        printf("║ 12. 加载数据                         ║\n");
        printf("║  0. 退出系统                         ║\n");
        printf("╚══════════════════════════════════════╝\n");
        printf("请选择操作: ");
        scanf("%d", &choice);
        getchar();  // 清空缓冲区中的换行符

        switch (choice) {
            case 1: ui_entry(ps);           break;
            case 2: ui_exit(ps);            break;
            case 3: ui_find(ps);            break;
            case 4: ui_free_spots(ps);      break;
            case 5: ui_wait_queue(ps);      break;
            case 6: ui_parking_map(ps);     break;
            case 7: ui_revenue(ps);         break;
            case 8: ui_transactions(ps);    break;
            case 9: ui_floor_usage(ps);     break;
            case 10: ui_peak_hours(ps);     break;
            case 11: ui_save(ps);           break;
            case 12: ui_load(ps);           break;
            case 0:
                printf("感谢使用，正在保存数据...\n");
                break;
            default:
                printf("无效选择，请重新输入！\n");
        }
    } while (choice != 0);
}

void ui_entry(ParkingSystem *ps) {
    char plate[16];
    printf("请输入车牌号: ");
    scanf("%s", plate);
    getchar();
    vehicle_entry(ps, plate);
}

void ui_exit(ParkingSystem *ps) {
    char plate[16];
    double fee;
    printf("请输入车牌号: ");
    scanf("%s", plate);
    getchar();
    vehicle_exit(ps, plate, &fee);
}

void ui_find(ParkingSystem *ps) {
    char plate[16];
    printf("请输入车牌号: ");
    scanf("%s", plate);
    getchar();

    VehicleRecord *rec = find_vehicle(ps, plate);
    if (rec != NULL) {
        char time_str[64];
        strftime(time_str, sizeof(time_str), "%Y-%m-%d %H:%M:%S",
                 localtime(&rec->entry_time));
        printf("车辆 %s 在场内\n", plate);
        printf("位置：%dF-%d排-%d列\n",
               rec->loc.floor+1, rec->loc.row+1, rec->loc.col+1);
        printf("入场时间：%s\n", time_str);
        return;
    }

    int qpos = find_in_queue(ps, plate);
    if (qpos != -1) {
        printf("车辆 %s 在等待队列中，排在第 %d 位\n", plate, qpos);
        return;
    }

    printf("未找到车辆 %s (可能已出场或不存在)\n", plate);
}

void ui_parking_map(ParkingSystem *ps) {
    printf("\n═══════════════════════════════════\n");
    printf("        立体停车场 — 车位状态图\n");
    printf("═══════════════════════════════════\n");
    printf("  ● 已占用   ○ 空闲\n\n");

    for (int f = FLOORS - 1; f >= 0; f--) {
        printf("─── %dF ", f + 1);
        if (f == FLOORS - 1) printf("(顶层)");
        printf(" ───\n");

        for (int r = 0; r < ROWS; r++) {
            printf("    ");
            for (int c = 0; c < COLS; c++) {
                if (ps->spots[f][r][c].status == SPOT_OCCUPIED)
                    printf("[●]");
                else
                    printf("[○]");
            }
            printf("\n");
        }
        int floor_used = 0;
        for (int r = 0; r < ROWS; r++)
            for (int c = 0; c < COLS; c++)
                if (ps->spots[f][r][c].status == SPOT_OCCUPIED) floor_used++;
        printf("    使用率: %d/%d (%.0f%%)\n\n",
               floor_used, ROWS*COLS, 100.0*floor_used/(ROWS*COLS));
    }

    printf("═══════════════════════════════════\n");
    int free_cnt = stack_size(&ps->free_spots);
    printf(" 空闲车位: %d / %d  |  排队车辆: %d\n",
           free_cnt, TOTAL_SPOTS, ps->wait_queue.count);
    printf("═══════════════════════════════════\n");
}
```

### 7.4 数据持久化 (file_io.c)

```c
// 文件魔数，用于校验文件类型
#define FILE_MAGIC 0x504B1204
#define FILE_VERSION 1

typedef struct {
    int magic;           // FILE_MAGIC
    int version;         // FILE_VERSION
    int history_count;   // 历史记录数
    int free_count;      // 空闲车位数（用于恢复栈）
    int queue_count;     // 队列长度
    double total_revenue;
    FeeRule fee_rule;
} FileHeader;

int save_data(ParkingSystem *ps, const char *filename) {
    FILE *fp = fopen(filename, "wb");
    if (fp == NULL) {
        printf("无法打开文件 %s 写入\n", filename);
        return 0;
    }

    // 1. 写文件头
    FileHeader header;
    header.magic = FILE_MAGIC;
    header.version = FILE_VERSION;
    header.history_count = ps->history_count;
    header.free_count = ps->free_spots.top + 1;
    header.queue_count = ps->wait_queue.count;
    header.total_revenue = ps->total_revenue;
    header.fee_rule = ps->fee_rule;
    fwrite(&header, sizeof(FileHeader), 1, fp);

    // 2. 写所有车位状态（spots 三维数组）
    fwrite(ps->spots, sizeof(ParkingSpot), FLOORS * ROWS * COLS, fp);

    // 3. 写空闲栈内容（所有坐标）
    fwrite(ps->free_spots.spots, sizeof(SpotLocation),
           ps->free_spots.top + 1, fp);

    // 4. 写等待队列
    QueueNode *p = ps->wait_queue.front;
    while (p != NULL) {
        fwrite(p, sizeof(QueueNode), 1, fp);
        p = p->next;
    }

    // 5. 写历史交易
    fwrite(ps->history, sizeof(TransactionRecord),
           ps->history_count, fp);

    fclose(fp);
    printf("数据已保存到 %s\n", filename);
    return 1;
}

int load_data(ParkingSystem *ps, const char *filename) {
    FILE *fp = fopen(filename, "rb");
    if (fp == NULL) {
        // 文件不存在不报错，使用初始状态
        return 0;
    }

    // 1. 读文件头
    FileHeader header;
    fread(&header, sizeof(FileHeader), 1, fp);

    // 校验魔数
    if (header.magic != FILE_MAGIC) {
        printf("文件格式错误！\n");
        fclose(fp);
        return 0;
    }

    // 2. 读车位状态
    fread(ps->spots, sizeof(ParkingSpot), FLOORS * ROWS * COLS, fp);

    // 3. 恢复空闲栈
    stack_init(&ps->free_spots, TOTAL_SPOTS);
    ps->free_spots.top = header.free_count - 1;
    fread(ps->free_spots.spots, sizeof(SpotLocation),
          header.free_count, fp);

    // 4. 恢复队列（逐个重建节点）
    queue_init(&ps->wait_queue);
    for (int i = 0; i < header.queue_count; i++) {
        QueueNode node;
        fread(&node, sizeof(QueueNode), 1, fp);
        queue_enqueue(&ps->wait_queue, node.plate, node.arrival_time);
    }

    // 5. 恢复历史记录
    ps->history_count = header.history_count;
    ps->history_capacity = header.history_count + 100;
    ps->history = (TransactionRecord *)malloc(
        sizeof(TransactionRecord) * ps->history_capacity);
    fread(ps->history, sizeof(TransactionRecord),
          header.history_count, fp);

    // 6. 恢复收入和计费规则
    ps->total_revenue = header.total_revenue;
    ps->fee_rule = header.fee_rule;

    fclose(fp);
    printf("数据已从 %s 加载\n", filename);
    return 1;
}
```

**注意**：读取后需要扫描 spots 数组重建哈希表（哈希表不持久化到文件），或在初始化时额外写一份 VehicleRecord。

**简化方案**（推荐）：不持久化哈希表，load 后扫描 spots：

```c
// 在 load_data 末尾或 ui_load 中重建哈希表
void rebuild_hash_from_spots(ParkingSystem *ps) {
    ht_clear(&ps->plate_index);
    for (int f = 0; f < FLOORS; f++)
        for (int r = 0; r < ROWS; r++)
            for (int c = 0; c < COLS; c++)
                if (ps->spots[f][r][c].status == SPOT_OCCUPIED) {
                    VehicleRecord *rec = malloc(sizeof(VehicleRecord));
                    strcpy(rec->plate, ps->spots[f][r][c].plate);
                    rec->loc = make_loc(f, r, c);
                    rec->entry_time = ...; // 直接从文件读或者用文件头存储
                    ht_insert(&ps->plate_index, rec);
                }
}
```

### 7.5 主程序入口 (main.c)

```c
#include "ds.h"
#include "parking.h"
#include "fee.h"
#include "stats.h"
#include "ui.h"
#include "file_io.h"

int main() {
    ParkingSystem ps;

    // 1. 初始化系统
    printf("正在初始化停车场系统...\n");
    ds_init(&ps);

    // 2. 尝试加载历史数据
    if (load_data(&ps, "parking_data.dat")) {
        // 重建哈希表（因为哈希表不是持久化的）
        for (int f = 0; f < FLOORS; f++)
            for (int r = 0; r < ROWS; r++)
                for (int c = 0; c < COLS; c++)
                    if (ps.spots[f][r][c].status == SPOT_OCCUPIED) {
                        VehicleRecord *rec = malloc(sizeof(VehicleRecord));
                        strcpy(rec->plate, ps.spots[f][r][c].plate);
                        rec->loc = make_loc(f, r, c);
                        rec->entry_time = time(NULL); // 简化：以当前时间为入场时间
                        ht_insert(&ps.plate_index, rec);
                    }
    }

    printf("停车场初始化完成！共 %d 个车位\n", TOTAL_SPOTS);

    // 3. 进入主菜单
    ui_main_menu(&ps);

    // 4. 退出前保存
    printf("正在保存数据...\n");
    save_data(&ps, "parking_data.dat");

    // 5. 释放所有内存
    ds_destroy(&ps);
    printf("系统已安全退出\n");

    return 0;
}
```

---

## 八、编译与运行

### 8.1 编译命令

```bash
# 编译所有源文件
gcc -o parking.exe ds.c parking.c fee.c stats.c ui.c file_io.c main.c -lm

# 或者分步编译
gcc -c ds.c -o ds.o
gcc -c parking.c -o parking.o
gcc -c fee.c -o fee.o
gcc -c stats.c -o stats.o
gcc -c ui.c -o ui.o
gcc -c file_io.c -o file_io.o
gcc -c main.c -o main.o
gcc -o parking.exe *.o -lm

# 运行
./parking.exe
```

### 8.2 编译注意事项

- `-lm` 链接数学库（`ceil` 函数需要）
- 注意文件编码统一为 UTF-8 或 GBK（输出中文）
- Windows 下可能需添加 `#define _CRT_SECURE_NO_WARNINGS` 避免 scanf 警告

---

## 九、接口契约（三人联调关键）

### pzx 提供给 yjk 和 zxc 的接口

| 函数 | 功能 | 被谁调用 |
|------|------|---------|
| `ds_init(ps)` | 初始化系统 | B, C (main) |
| `ds_destroy(ps)` | 销毁系统 | C (main) |
| `stack_push(s, loc)` | 空闲车位入栈 | B |
| `stack_pop(s)` | 分配空闲车位 | B |
| `stack_is_empty(s)` | 检查是否有空位 | B, C |
| `stack_size(s)` | 空闲车位数 | C |
| `queue_enqueue(q, plate, t)` | 加入排队 | B |
| `queue_dequeue(q, out_plate, out_t)` | 排队出队 | B |
| `queue_is_empty(q)` | 检查排队是否空 | B |
| `queue_count(q)` | 排队人数 | C |
| `ht_insert(ht, rec)` | 哈希表插入 | B |
| `ht_find(ht, plate)` | 哈希表查找 | B, C |
| `ht_remove(ht, plate)` | 哈希表删除 | B |
| `make_loc(f, r, c)` | 创建坐标 | B, C |

### yjk 提供给 zxc 的接口

| 函数 | 功能 |
|------|------|
| `vehicle_entry(ps, plate)` | 入场登记 |
| `vehicle_exit(ps, plate, &fee)` | 出场结算 |
| `find_vehicle(ps, plate)` | 查找在场车辆 |
| `find_in_queue(ps, plate)` | 查找排队位置 |
| `set_fee_rule(ps, rule)` | 设置计费规则 |
| `calculate_fee(&rule, minutes)` | 计算费用 |

### zxc 依赖的接口

zxc 调用 pzx 的：`ds_init`, `ds_destroy`, `stack_is_empty`, `stack_size`, `queue_count`, `ht_find`

zxc 调用 yjk 的：`vehicle_entry`, `vehicle_exit`, `find_vehicle`, `find_in_queue`

zxc 自己实现：所有 `stats_*`, `ui_*`, `save_data`, `load_data`, `main`

---

## 十、开发顺序建议

### 第一轮（并行开发）

| 人员 | 任务 |
|------|------|
| **pzx** | 完成 `ds.h` 和 `ds.c` —— 所有结构体定义 + 栈/队列/哈希表全部实现 |
| **zxc** | 完成 `ui.h` 和 `ui.c` 的菜单框架 + `file_io.h`/`file_io.c` 框架 |
| **yjk** | 阅读 ds.h 了解接口，设计 parking.c 和 fee.c 的内部逻辑 |

### 第二轮（并行开发）

| 人员 | 任务 |
|------|------|
| **pzx** | 自测 ds.c 所有函数（写测试主函数验证栈/队列/哈希表） |
| **yjk** | 基于 ds.h 开发 `parking.c` + `fee.c` 全部业务逻辑 |
| **zxc** | 完成 `stats.c` + `file_io.c` 全部实现 + `main.c` |

### 第三轮（三人联调）

1. 编译所有文件，修复编译错误（多为接口不匹配）
2. 运行测试场景验证功能
3. 优化输出格式和用户体验

---

## 十一、测试场景

### 场景 1：基本入场/出场

```
操作：入场 5 辆不同车辆
预期：分配 5 个车位，空闲车位减少
```

### 场景 2：停车场满 + 排队

```
操作：停满 60 辆 → 再入场第 61 辆
预期：第 61 辆进入等待队列，提示"第1位"
```

### 场景 3：出一个进一个

```
操作：出场 1 辆
预期：排队车辆自动入场，显示入场信息，队列长度减 1
```

### 场景 4：计费验证

```
操作：车辆入场 → 等待 1 分钟 → 出场
预期：停车 1 分钟 < 免费 15 分钟 → 费用 = 0 元
```

### 场景 5：统计功能

```
操作：多次入场/出场 → 查看收入统计 → 查看楼层热度
预期：统计数据与操作一致
```

### 场景 6：数据持久化

```
操作：保存 → 退出 → 重新启动 → 加载
预期：入场车辆和交易记录恢复
```

### 场景 7：查找功能

```
操作：查找在场车辆 → 查找排队车辆 → 查找已出场车辆
预期：在场显示位置，排队显示序号，已出场提示未找到
```

---

## 十二、验收清单

| # | 功能 | 说明 | 负责 |
|---|------|------|------|
| 1 | 系统初始化 | 创建 60 个空闲车位 | **pzx** |
| 2 | 空闲栈 | pop/push 操作正确 | **pzx** |
| 3 | 哈希表 | 插入/查找/删除 O(1) | **pzx** |
| 4 | 等待队列 | 入队/出队 FIFO | **pzx** |
| 5 | 入场登记 | 有空位分配，无空位排队 | **yjk** |
| 6 | 出场结算 | 释放车位，计算费用 | **yjk** |
| 7 | 排队调度 | "出一个进一个" | **yjk** |
| 8 | 实时查找 | 查找在场 / 排队车辆 | **yjk** |
| 9 | 计费逻辑 | 免停 + ceil小时 + 封顶 | **yjk** |
| 10 | 车位状态图 | ASCII 图显示占用/空闲 | **zxc** |
| 11 | 收入统计 | 总收入 / 今日 / 本月 | **zxc** |
| 12 | 交易明细 | 历史记录分页显示 | **zxc** |
| 13 | 楼层热度 | 各层周转率排名 | **zxc** |
| 14 | 峰值时段 | 各小时入场统计 | **zxc** |
| 15 | 数据持久化 | 二进制文件保存/加载 | **zxc** |
| 16 | 菜单界面 | 循环菜单，各功能入口 | **zxc** |
| 17 | 内存管理 | 无内存泄漏 | **pzx, zxc** |

---

## 十三、代码规范（三人统一）

### 命名规范

| 类别 | 规范 | 示例 |
|------|------|------|
| 结构体 | 大驼峰 | `VehicleRecord`, `ParkingSpot` |
| 枚举 | 大写下划线 | `SPOT_FREE`, `ENTRY_SUCCESS` |
| 函数 | 小写下划线 | `vehicle_entry`, `ht_find` |
| 变量 | 小写下划线 | `free_count`, `entry_time` |
| 宏 | 大写下划线 | `TOTAL_SPOTS`, `HT_SIZE` |
| 类型 | `typedef` + 大驼峰 | `ParkingSystem`, `HashMap` |

### 注释规范

每个函数前必须有块注释：

```c
/**
 * 函数功能描述（一句话）
 *
 * @param ps    停车场系统指针
 * @param plate 车牌号
 * @return      入场结果枚举
 *               - ENTRY_SUCCESS: 成功分配车位
 *               - PARKING_FULL:  已满，已加入排队
 *               - PLATE_EXISTS:  车牌重复
 *
 * 逻辑说明：xxx
 */
```

### 错误处理

1. `malloc` / `realloc` 后检查 `== NULL`
2. `fopen` 后检查 `== NULL`
3. 函数参数做基础校验（非 NULL）
4. 哈希表查找不到返回 NULL，不崩溃
5. 队列为空时 `dequeue` 返回 0，不崩溃

---

> 本文档为三人分工开发的完整方案。每个人按照各自章节实现，最后按接口契约集成即可。
